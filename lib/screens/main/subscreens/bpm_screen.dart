import 'package:flutter/material.dart';

class BPMScreen extends StatelessWidget {
  const BPMScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Blood Pressure Meter")),
      body: Container(),
    );
  }
}
